const { test, expect } = require('@playwright/test');

test.describe('Import Clients API Tests @api @regression', () => {
  test.skip('POST Upload Client Data payload @TC-603', async () => {
    // Placeholder for importing clients via API
  });
});
